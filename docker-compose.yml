services:
  web:
    build: .
    image: hamclock:latest
    ports:
      - "8091:8081"
    env_file: YO3BEE.env
    volumes:
      - hamclock-data:/root/.hamclock
    restart: unless-stopped

volumes:
  hamclock-data:




